# Leave empty when using `npm run dev` (Vite proxies /api → backend).
# For production builds that call another origin, use your deployed API URL, e.g.:
# VITE_API_BASE=https://your-api.example.com

VITE_API_BASE=
